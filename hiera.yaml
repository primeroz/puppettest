---
:backends:
  - yaml

:logger: console

:hierarchy:
  - "node/%{::fqdn}"
  - "network/%{::network_range}"
  - "domain/%{::domain}"
  - "os/%{::osfamily}"
  - asterisk
  - zol
  - common

:yaml:
   :datadir: /etc/puppet/hiera
